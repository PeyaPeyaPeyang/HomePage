name: CI

on:
  push:
    branches: ["develop", "production"]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.number || github.sha }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    name: Build with Node.js 16, ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup pnpm
        uses: pnpm/action-setup@v2

      - name: Setup Node.js 16
        uses: actions/setup-node@v3
        with:
          node-version: 16
          cache: pnpm

      - name: Install dependencies
        run: pnpm install

      - name: Build
        run: pnpm run build

  lint:
    runs-on: ubuntu-latest

    permissions:
      security-events: write

    name: Lint with Node.js 16, ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v2

      - name: Setup Node.js 16
        uses: actions/setup-node@v3
        with:
          node-version: 16
          cache: pnpm

      - name: Install dependencies
        run: pnpm install

      - name: Build
        run: pnpm run build

      - name: Cache ESLint results
        uses: actions/cache@v3
        with:
          path: ${{ github.workspace }}/.eslintcache
          key: ${{ runner.os }}-eslint-${{ hashFiles('.eslintcache') }}
          restore-keys: |
            ${{ runner.os }}-eslint-

      - name: Lint
        run: pnpm run lint:sarif

      - name: Upload lint results to GitHub repository
        uses: github/codeql-action/upload-sarif@v2
        with:
          sarif_file: results.sarif

      - name: Cache Prettier results
        uses: actions/cache@v3
        with:
          path: ${{ github.workspace }}/node_modules/.cache/prettier/.prettier-cache
          key: ${{ runner.os }}-prettier-${{ hashFiles('**/node_modules/.cache/prettier/.prettier-cache') }}
          restore-keys: |
            ${{ runner.os }}-prettier-

      - name: Check formatting
        run: pnpm check:format

      - name: Cache TypeScript incremental building results
        uses: actions/cache@v3
        with:
          path: ${{ github.workspace }}/src/tsconfig.tsbuildinfo
          key: ${{ runner.os }}-typescript-incremental-${{ hashFiles('**/*.tsbuildinfo') }}
          restore-keys: |
            ${{ runner.os }}-typescript-incremental-

      - name: Typecheck
        run: pnpm check:type
