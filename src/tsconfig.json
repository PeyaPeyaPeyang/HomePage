{
    "extends": "../tsconfig.base.json",
    "compilerOptions": {
        "allowJs": true,
        "downlevelIteration": true,
        "lib": ["ESNext", "DOM", "DOM.Iterable"],
        "paths": {
            "@/*": ["./*"]
        }
    },
    "include": ["./", "../types"]
}
