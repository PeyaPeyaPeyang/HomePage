{
    "extends": "../tsconfig.base.json",
    "compilerOptions": {
        "allowJs": true,
        "downlevelIteration": true,
        "incremental": true,
        "lib": ["ESNext", "DOM", "DOM.Iterable"],
        "paths": {
            "@/*": ["./*"]
        }
    },
    "include": ["./", "../types"]
}
